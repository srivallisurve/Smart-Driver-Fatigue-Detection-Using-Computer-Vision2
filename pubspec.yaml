name: drivers_drowsiness_frontend
description: Driver Drowsiness Detection

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  audioplayers: ^5.0.0
  flutter_web_plugins:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/alarm.wav